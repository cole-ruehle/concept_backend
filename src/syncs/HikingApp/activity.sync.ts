/**
 * Activity Recording Synchronizations
 * 
 * These synchronizations automatically record user activities in UserHistory.
 * 
 * Note: This file previously contained syncs for deprecated concepts:
 * - DynamicExitPlanner (removed - replaced by LLMRoutePlanner)
 * - TransitRoutePlanner (removed - replaced by LLMRoutePlanner)
 * 
 * Activity recording for route planning is now handled in llmRoutePlanner.sync.ts
 * via the LLMRoutePlanningActivityRecording sync.
 */

// This file is kept for potential future activity recording syncs
// but currently has no active synchronizations.

export {};
